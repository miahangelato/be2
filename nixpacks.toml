[phases.install]
cmds = ["python -m venv --copies /opt/venv", ". /opt/venv/bin/activate"]

[phases.build]
cmds = [
  "pip install --upgrade pip",
  "pip install --no-cache-dir -r requirements-railway.txt",
  "python railway_build.py"
]

[phases.start]
cmd = "python startup_simple.py"